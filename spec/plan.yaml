# hfs3 Rust Rewrite — DAG Execution Plan
# Canonical source of truth for the parallel build.
# 11 agents across 5 levels.

project:
  name: hfs3
  description: |
    Rewrite of HuggingFace-to-S3 mirror tool from Python to Rust.
    Single binary with three subcommands: mirror, pull, run.
    Streaming architecture — zero-copy pipe from HF REST API to S3 multipart upload.
    Memory-aware concurrency based on /proc/meminfo.
  repo: .
  binary_name: hfs3

scope:
  in:
    - Full Rust CLI replacing Python implementation
    - HF REST API client (reqwest, no HF crate)
    - Streaming HF-to-S3 multipart upload (zero intermediate files)
    - Adaptive chunk sizing (8MB / 64MB / 128MB by file size)
    - Memory-aware concurrency (/proc/meminfo)
    - S3-to-local pull with concurrent downloads
    - Docker build+run for pulled repos
    - JSON summary to stdout, human progress to stderr
    - Devcontainer with Rust toolchain
    - GitHub Actions CI
  out:
    - Auth / multi-user
    - Web UI
    - Windows support
    - Cross-compiled release builds (cross/cargo-zigbuild)
    - Caching / incremental sync
    - HF private repo support beyond token auth
    - S3 server-side encryption configuration
    - Any Python code remaining

stack:
  language: Rust (2021 edition)
  async_runtime: tokio (multi-threaded)
  http_client: reqwest (with stream feature)
  s3: aws-sdk-s3
  cli: clap (derive)
  serialization: serde + serde_json
  logging: tracing + tracing-subscriber
  testing: built-in (#[cfg(test)] + tokio::test)
  task_runner: just
  ci: GitHub Actions
  container: devcontainer (Rust toolchain + just + awscli)

env_vars:
  - name: HFS3_S3_BUCKET
    required: true
    description: S3 bucket for mirrored repos
  - name: HFS3_S3_PREFIX
    required: false
    default: hfs3-mirror
    description: Key prefix within the bucket
  - name: HF_TOKEN
    required: false
    description: HuggingFace auth token for gated repos
  - name: AWS_REGION
    required: false
    description: AWS region for S3 client

# ============================================================
# DAG Adjacency List
# ============================================================
agents:
  - id: A0
    name: scaffold
    level: 0
    depends_on: []
    input_protos: []
    output_protos: [ScaffoldOutput]
    reads: [justfile, pyproject.toml, .gitignore, AGENTS.md]
    writes:
      - Cargo.toml
      - src/main.rs
      - src/lib.rs
      - justfile
      - .devcontainer/devcontainer.json
      - .devcontainer/Dockerfile
      - .gitignore
      - AGENTS.md
    test_glob: none
    completion_criteria:
      - "cargo build compiles successfully (empty subcommands)"
      - "just build runs cargo build"
      - "just test runs cargo test"
      - "Python files deleted: src/hfs3/, pyproject.toml, uv.lock, .python-version"
      - ".gitignore includes /target, drops Python entries"
      - "AGENTS.md updated for Rust world"
      - "Devcontainer Dockerfile installs rustup, just, awscli"
      - "Binary name is hfs3 in Cargo.toml"
    agent_prompt: |
      You are building the project scaffold for the hfs3 Rust rewrite.

      ## What to do

      1. DELETE these Python artifacts:
         - src/hfs3/ (entire directory)
         - pyproject.toml
         - uv.lock
         - .python-version
         - .venv/ (if present)
         - .ruff_cache/ (if present)
         - .pytest_cache/ (if present)
         DO NOT delete tests/ — keep it as behavioral spec reference.

      2. CREATE Cargo.toml at repo root:
         ```toml
         [package]
         name = "hfs3"
         version = "0.1.0"
         edition = "2021"
         description = "Mirror HuggingFace repos to S3 for air-gapped deployment"

         [[bin]]
         name = "hfs3"
         path = "src/main.rs"

         [dependencies]
         tokio = { version = "1", features = ["full"] }
         reqwest = { version = "0.12", features = ["stream", "json"] }
         aws-sdk-s3 = "1"
         aws-config = { version = "1", features = ["behavior-version-latest"] }
         clap = { version = "4", features = ["derive"] }
         serde = { version = "1", features = ["derive"] }
         serde_json = "1"
         tracing = "0.1"
         tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
         thiserror = "2"
         futures = "0.3"
         bytes = "1"
         ```

      3. CREATE src/main.rs — thin entrypoint that delegates to cli module:
         ```rust
         //! hfs3 binary entrypoint.
         //! All logic lives in the library crate (src/lib.rs).
         //! CLI wiring is in src/cli.rs (implemented by agent A8).

         #[tokio::main]
         async fn main() {
             if let Err(e) = hfs3::cli::run().await {
                 eprintln!("Error: {e}");
                 std::process::exit(1);
             }
         }
         ```

      4. CREATE src/lib.rs — module declarations (empty stubs for now):
         ```rust
         pub mod cli;
         pub mod concurrency;
         pub mod config;
         pub mod docker;
         pub mod error;
         pub mod hf;
         pub mod pipeline;
         pub mod s3;
         pub mod types;
         ```
         Also create empty placeholder files for each module:
         src/cli.rs, src/config.rs, src/concurrency.rs, src/error.rs, src/types.rs,
         src/hf.rs, src/s3.rs, src/pipeline.rs, src/docker.rs

         Each file should contain a stub public function so the crate compiles:
         ```rust
         // Implemented by agent Ax
         ```

         CRITICAL: src/cli.rs must have a compilable stub:
         ```rust
         // Implemented by agent A8
         pub async fn run() -> Result<(), Box<dyn std::error::Error>> {
             eprintln!("CLI not yet wired");
             Ok(())
         }
         ```

      5. REWRITE justfile for Rust:
         ```just
         # hfs3 — Mirror HuggingFace to S3

         default:
             @just --list

         build:
             cargo build

         build-release:
             cargo build --release

         test *args='':
             cargo test {{args}}

         test-v *args='':
             cargo test {{args}} -- --nocapture

         check:
             cargo check

         clippy:
             cargo clippy -- -D warnings

         fmt:
             cargo fmt

         fmt-check:
             cargo fmt -- --check

         lint: fmt-check clippy

         clean:
             cargo clean

         # Main workflows (require compiled binary)
         mirror repo:
             cargo run -- mirror "{{repo}}"

         pull repo dest='./repo':
             cargo run -- pull "{{repo}}" --dest "{{dest}}"

         run repo dest='./repo' port='7860':
             cargo run -- run "{{repo}}" --dest "{{dest}}" --port "{{port}}"
         ```

      6. UPDATE .gitignore:
         ```
         /target
         *.swp
         *.swo
         .DS_Store
         repo/
         ```
         Remove all Python-specific entries (__pycache__, .venv, etc.)

      7. UPDATE AGENTS.md for the Rust world. Keep the same structure
         (What this is, Commands, Architecture, Env vars, Testing conventions, Tooling)
         but update all content to reflect Rust modules, just commands, and the
         streaming architecture. Mention tests/ still exists as Python behavioral spec.

      8. CREATE .devcontainer/Dockerfile:
         ```dockerfile
         FROM mcr.microsoft.com/devcontainers/rust:1-bookworm

         RUN apt-get update && apt-get install -y \
             awscli \
             pkg-config \
             libssl-dev \
             && rm -rf /var/lib/apt/lists/*

         # Install just
         RUN curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash -s -- --to /usr/local/bin

         # Pre-build dependencies (cache layer)
         WORKDIR /workspaces/hfs3
         ```

      9. CREATE .devcontainer/devcontainer.json:
         ```json
         {
           "name": "hfs3",
           "build": {
             "dockerfile": "Dockerfile",
             "context": ".."
           },
           "postCreateCommand": "cargo build",
           "customizations": {
             "vscode": {
               "extensions": [
                 "rust-lang.rust-analyzer",
                 "tamasfe.even-better-toml",
                 "serayuzgur.crates"
               ]
             }
           },
           "mounts": [
             "source=${localEnv:HOME}/.cargo/registry,target=/usr/local/cargo/registry,type=bind,consistency=cached"
           ],
           "remoteUser": "vscode",
           "features": {
             "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
           },
           "initializeCommand": "git clone https://github.com/kaashmonee/dotfiles /tmp/dotfiles 2>/dev/null || true"
         }
         ```

      ## Validation
      - `cargo build` succeeds
      - `just build` runs without error
      - `ls src/hfs3/` fails (Python deleted)
      - `test ! -f pyproject.toml` succeeds
      - Binary name in `cargo build` output is `hfs3`

  - id: A1
    name: types_and_errors
    level: 0
    depends_on: []
    input_protos: []
    output_protos: [TypesOutput]
    reads: [tests/test_hf.py]
    writes:
      - src/types.rs
      - src/error.rs
    test_glob: "types::tests,error::tests"
    completion_criteria:
      - "RepoRef struct with repo_id: String, repo_type: RepoType, revision: String"
      - "RepoType enum with Model, Dataset, Space variants"
      - "RepoType implements Display (model, dataset, space) and FromStr"
      - "parse_repo_url function handles: bare ID, full URL, spaces, datasets, revision, trailing slash, whitespace"
      - "parse_repo_url returns Err for missing slash (matches Python test_invalid_no_slash)"
      - "HfFileEntry struct with path: String, size: u64, oid: String"
      - "MirrorResult struct with repo: RepoRef, bucket: String, prefix: String, files_transferred: usize, bytes_transferred: u64, duration_secs: f64"
      - "PullResult struct with repo: RepoRef, dest: PathBuf, files_downloaded: usize, bytes_downloaded: u64, duration_secs: f64"
      - "Hfs3Error enum using thiserror with variants: Config, HfApi, S3, Io, Docker, Parse"
      - "All 9 test cases from Python test_hf.py pass as Rust unit tests"
    agent_prompt: |
      You are implementing the shared domain types and error types for hfs3.

      ## Files to write

      ### src/types.rs

      Define these types:

      ```rust
      use std::path::PathBuf;
      use std::str::FromStr;
      use std::fmt;
      use serde::{Serialize, Deserialize};

      /// HuggingFace repo type.
      #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
      pub enum RepoType {
          Model,
          Dataset,
          Space,
      }

      impl fmt::Display for RepoType {
          fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
              match self {
                  RepoType::Model => write!(f, "model"),
                  RepoType::Dataset => write!(f, "dataset"),
                  RepoType::Space => write!(f, "space"),
              }
          }
      }

      impl FromStr for RepoType {
          type Err = String;
          fn from_str(s: &str) -> Result<Self, Self::Err> {
              match s {
                  "model" => Ok(RepoType::Model),
                  "dataset" => Ok(RepoType::Dataset),
                  "space" => Ok(RepoType::Space),
                  other => Err(format!("unknown repo type: {other}")),
              }
          }
      }

      /// Parsed HuggingFace repo reference.
      #[derive(Debug, Clone, PartialEq, Eq)]
      pub struct RepoRef {
          pub repo_id: String,    // e.g. "meta-llama/Llama-2-7b"
          pub repo_type: RepoType, // Model, Dataset, Space
          pub revision: String,    // git revision, default "main"
      }

      /// A single file entry from the HF tree API.
      #[derive(Debug, Clone, Serialize, Deserialize)]
      pub struct HfFileEntry {
          pub path: String,       // relative path within repo
          pub size: u64,          // file size in bytes
          pub oid: String,        // git object ID (sha256)
      }

      /// Result of a mirror operation, serialized as JSON to stdout.
      #[derive(Debug, Serialize)]
      pub struct MirrorResult {
          pub repo_id: String,
          pub repo_type: String,
          pub bucket: String,
          pub prefix: String,
          pub files_transferred: usize,
          pub bytes_transferred: u64,
          pub duration_secs: f64,
      }

      /// Result of a pull operation, serialized as JSON to stdout.
      #[derive(Debug, Serialize)]
      pub struct PullResult {
          pub repo_id: String,
          pub repo_type: String,
          pub dest: PathBuf,
          pub files_downloaded: usize,
          pub bytes_downloaded: u64,
          pub duration_secs: f64,
      }

      /// Result of a run operation, serialized as JSON to stdout.
      #[derive(Debug, Serialize)]
      pub struct RunResult {
          pub repo_id: String,
          pub repo_type: String,
          pub image_tag: String,
          pub port: u16,
      }
      ```

      Implement `parse_repo_url`:
      ```rust
      /// Parse a HF URL or bare repo ID into a RepoRef.
      ///
      /// Accepts:
      ///   - "meta-llama/Llama-2-7b"                         -> Model
      ///   - "https://huggingface.co/meta-llama/Llama-2-7b"  -> Model
      ///   - "https://huggingface.co/spaces/user/my-space"   -> Space
      ///   - "https://huggingface.co/datasets/user/my-ds"    -> Dataset
      ///   - URLs with /tree/revision suffix
      pub fn parse_repo_url(url_or_id: &str) -> Result<RepoRef, crate::error::Hfs3Error> {
          // 1. Trim whitespace and trailing slash
          // 2. Strip https://huggingface.co/ prefix if present
          // 3. Detect repo_type from path prefix (spaces/, datasets/)
          // 4. Extract revision from /tree/xxx suffix
          // 5. Validate repo_id contains a slash (owner/name)
          // ... implement the same logic as Python hf.py parse_repo_url
      }
      ```

      ### src/error.rs

      ```rust
      use thiserror::Error;

      #[derive(Error, Debug)]
      pub enum Hfs3Error {
          #[error("config error: {0}")]
          Config(String),

          #[error("HuggingFace API error: {0}")]
          HfApi(String),

          #[error("S3 error: {0}")]
          S3(String),

          #[error("I/O error: {0}")]
          Io(#[from] std::io::Error),

          #[error("Docker error: {0}")]
          Docker(String),

          #[error("parse error: {0}")]
          Parse(String),

          #[error("HTTP error: {0}")]
          Http(#[from] reqwest::Error),
      }
      ```

      ## Tests

      Port ALL 9 test cases from tests/test_hf.py to Rust unit tests in src/types.rs:

      ```rust
      #[cfg(test)]
      mod tests {
          use super::*;

          #[test]
          fn test_bare_model_id() {
              let ref_ = parse_repo_url("meta-llama/Llama-2-7b").unwrap();
              assert_eq!(ref_.repo_id, "meta-llama/Llama-2-7b");
              assert_eq!(ref_.repo_type, RepoType::Model);
              assert_eq!(ref_.revision, "main");
          }

          #[test]
          fn test_model_url() { /* https://huggingface.co/meta-llama/Llama-2-7b */ }

          #[test]
          fn test_space_url() { /* spaces/user/my-space -> Space */ }

          #[test]
          fn test_dataset_url() { /* datasets/user/my-ds -> Dataset */ }

          #[test]
          fn test_url_with_revision() { /* /tree/dev -> revision="dev" */ }

          #[test]
          fn test_trailing_slash_stripped() { /* spaces/user/my-space/ */ }

          #[test]
          fn test_whitespace_stripped() { /* "  meta-llama/Llama-2-7b  " */ }

          #[test]
          fn test_invalid_no_slash() {
              assert!(parse_repo_url("just-a-name").is_err());
          }

          #[test]
          fn test_invalid_bare_namespace() {
              assert!(parse_repo_url("https://huggingface.co/spaces/onlynamespace").is_err());
          }
      }
      ```

      Also add tests for RepoType Display/FromStr and error variant construction.

  - id: A2
    name: config
    level: 0
    depends_on: []
    input_protos: []
    output_protos: [ConfigOutput]
    reads: [tests/test_config.py]
    writes:
      - src/config.rs
    test_glob: "config::tests"
    completion_criteria:
      - "AppConfig struct with s3_bucket: String, s3_prefix: String, hf_token: Option<String>, aws_region: Option<String>"
      - "AppConfig::from_env() reads from std::env"
      - "Missing HFS3_S3_BUCKET returns Err with clear message"
      - "HFS3_S3_PREFIX defaults to 'hfs3-mirror'"
      - "HF_TOKEN and AWS_REGION are optional"
      - "All 3 test cases from Python test_config.py pass as Rust unit tests"
    agent_prompt: |
      You are implementing the config module for hfs3.

      ## File to write

      ### src/config.rs

      ```rust
      use crate::error::Hfs3Error;

      /// Runtime configuration. All fields sourced from environment variables.
      #[derive(Debug, Clone)]
      pub struct AppConfig {
          /// S3 bucket for mirrored repos (required).
          pub s3_bucket: String,
          /// Key prefix within the bucket. Default: "hfs3-mirror".
          pub s3_prefix: String,
          /// HuggingFace auth token for gated repos (optional).
          pub hf_token: Option<String>,
          /// AWS region for S3 client (optional, uses SDK default if unset).
          pub aws_region: Option<String>,
      }

      impl AppConfig {
          /// Build config from environment variables.
          ///
          /// Required: HFS3_S3_BUCKET
          /// Optional: HFS3_S3_PREFIX (default "hfs3-mirror"), HF_TOKEN, AWS_REGION
          pub fn from_env() -> Result<Self, Hfs3Error> {
              let s3_bucket = std::env::var("HFS3_S3_BUCKET")
                  .map_err(|_| Hfs3Error::Config("HFS3_S3_BUCKET is required".into()))?;

              Ok(Self {
                  s3_bucket,
                  s3_prefix: std::env::var("HFS3_S3_PREFIX")
                      .unwrap_or_else(|_| "hfs3-mirror".into()),
                  hf_token: std::env::var("HF_TOKEN").ok(),
                  aws_region: std::env::var("AWS_REGION").ok(),
              })
          }

          /// S3 key prefix for a given repo ref.
          /// Format: {s3_prefix}/{repo_type}/{owner}--{name}
          pub fn s3_prefix_for(&self, repo_type: &str, repo_id: &str) -> String {
              format!(
                  "{}/{}/{}",
                  self.s3_prefix,
                  repo_type,
                  repo_id.replace('/', "--")
              )
          }
      }
      ```

      ## Tests

      Port all 3 test cases from tests/test_config.py:

      ```rust
      #[cfg(test)]
      mod tests {
          use super::*;
          use std::env;

          // Helper: run test with specific env vars set, restore after
          fn with_env<F: FnOnce()>(vars: &[(&str, Option<&str>)], f: F) {
              // Save originals
              let originals: Vec<_> = vars.iter().map(|(k, _)| (*k, env::var(k).ok())).collect();
              // Set/unset
              for (k, v) in vars {
                  match v {
                      Some(val) => env::set_var(k, val),
                      None => env::remove_var(k),
                  }
              }
              f();
              // Restore
              for (k, orig) in originals {
                  match orig {
                      Some(val) => env::set_var(k, val),
                      None => env::remove_var(k),
                  }
              }
          }

          #[test]
          fn test_from_env_minimal() {
              with_env(&[
                  ("HFS3_S3_BUCKET", Some("my-bucket")),
                  ("HFS3_S3_PREFIX", None),
                  ("HF_TOKEN", None),
                  ("AWS_REGION", None),
              ], || {
                  let cfg = AppConfig::from_env().unwrap();
                  assert_eq!(cfg.s3_bucket, "my-bucket");
                  assert_eq!(cfg.s3_prefix, "hfs3-mirror");
                  assert!(cfg.hf_token.is_none());
              });
          }

          #[test]
          fn test_from_env_full() {
              with_env(&[
                  ("HFS3_S3_BUCKET", Some("prod-bucket")),
                  ("HFS3_S3_PREFIX", Some("custom/prefix")),
                  ("HF_TOKEN", Some("hf_abc123")),
                  ("AWS_REGION", Some("us-west-2")),
              ], || {
                  let cfg = AppConfig::from_env().unwrap();
                  assert_eq!(cfg.s3_bucket, "prod-bucket");
                  assert_eq!(cfg.s3_prefix, "custom/prefix");
                  assert_eq!(cfg.hf_token.as_deref(), Some("hf_abc123"));
                  assert_eq!(cfg.aws_region.as_deref(), Some("us-west-2"));
              });
          }

          #[test]
          fn test_missing_bucket_raises() {
              with_env(&[("HFS3_S3_BUCKET", None)], || {
                  let err = AppConfig::from_env().unwrap_err();
                  assert!(err.to_string().contains("HFS3_S3_BUCKET"));
              });
          }

          #[test]
          fn test_s3_prefix_for() {
              let cfg = AppConfig {
                  s3_bucket: "b".into(),
                  s3_prefix: "hfs3-mirror".into(),
                  hf_token: None,
                  aws_region: None,
              };
              assert_eq!(
                  cfg.s3_prefix_for("model", "meta-llama/Llama-2-7b"),
                  "hfs3-mirror/model/meta-llama--Llama-2-7b"
              );
          }
      }
      ```

      IMPORTANT: Config tests mutate environment variables. Rust runs tests in
      parallel by default. Use the `with_env` helper to set/restore vars, and
      note that these tests should ideally run with `--test-threads=1` or use
      a mutex. Add a comment about this. The simplest approach: use a
      `static MUTEX: std::sync::Mutex<()>` that each test locks.

  - id: A3
    name: hf_client
    level: 1
    depends_on: [A1]
    input_protos: [TypesOutput]
    output_protos: [HfClientOutput]
    reads: [src/types.rs, src/error.rs]
    writes:
      - src/hf.rs
    test_glob: "hf::tests"
    completion_criteria:
      - "list_repo_files(client, repo_ref, token) -> Result<Vec<HfFileEntry>> using HF REST API"
      - "Hits /api/models/{repo}/tree/{rev} for models, /api/datasets/... for datasets, /api/spaces/... for spaces"
      - "Recursively lists all files (HF tree API is recursive=true or paginated)"
      - "Handles pagination if HF API returns paginated results"
      - "Sets Authorization header when HF_TOKEN is provided"
      - "download_file_stream(client, repo_ref, file_path, token) -> impl Stream<Item=Bytes>"
      - "Returns a reqwest byte stream for zero-copy piping to S3"
      - "Uses resolve endpoint: https://huggingface.co/{repo_id}/resolve/{revision}/{path}"
      - "Unit tests mock the HTTP layer (no real HF calls in unit tests)"
    agent_prompt: |
      You are implementing the HuggingFace REST API client for hfs3.

      ## Dependencies
      Read src/types.rs for RepoRef, RepoType, HfFileEntry.
      Read src/error.rs for Hfs3Error.

      ## File to write

      ### src/hf.rs

      Two main functions:

      ### 1. list_repo_files

      ```rust
      use crate::types::{RepoRef, RepoType, HfFileEntry};
      use crate::error::Hfs3Error;
      use reqwest::Client;

      /// List all files in a HuggingFace repo via the REST API.
      ///
      /// API endpoints:
      ///   Models:   GET https://huggingface.co/api/models/{repo_id}/tree/{revision}
      ///   Datasets: GET https://huggingface.co/api/datasets/{repo_id}/tree/{revision}
      ///   Spaces:   GET https://huggingface.co/api/spaces/{repo_id}/tree/{revision}
      ///
      /// Query params: recursive=true
      /// Auth: Bearer {token} header if token is Some
      ///
      /// Response is JSON array of objects with fields:
      ///   { "type": "file"|"directory", "path": "...", "size": N, "oid": "..." }
      ///
      /// Filter to type=="file" only. Return Vec<HfFileEntry>.
      pub async fn list_repo_files(
          client: &Client,
          repo: &RepoRef,
          token: Option<&str>,
      ) -> Result<Vec<HfFileEntry>, Hfs3Error> {
          let api_prefix = match repo.repo_type {
              RepoType::Model => "models",
              RepoType::Dataset => "datasets",
              RepoType::Space => "spaces",
          };
          let url = format!(
              "https://huggingface.co/api/{}/{}/tree/{}",
              api_prefix, repo.repo_id, repo.revision
          );

          let mut req = client.get(&url).query(&[("recursive", "true")]);
          if let Some(t) = token {
              req = req.bearer_auth(t);
          }

          let resp = req.send().await.map_err(Hfs3Error::Http)?;
          if !resp.status().is_success() {
              return Err(Hfs3Error::HfApi(format!(
                  "HF API returned {} for {}",
                  resp.status(), url
              )));
          }

          // Parse response — array of HF tree entries
          #[derive(serde::Deserialize)]
          struct HfTreeEntry {
              #[serde(rename = "type")]
              entry_type: String,
              path: String,
              #[serde(default)]
              size: u64,
              #[serde(default)]
              oid: String,
          }

          let entries: Vec<HfTreeEntry> = resp.json().await.map_err(Hfs3Error::Http)?;

          Ok(entries
              .into_iter()
              .filter(|e| e.entry_type == "file")
              .map(|e| HfFileEntry {
                  path: e.path,
                  size: e.size,
                  oid: e.oid,
              })
              .collect())
      }
      ```

      ### 2. download_file_stream

      ```rust
      use bytes::Bytes;
      use futures::Stream;

      /// Get a streaming byte response for a single file from HuggingFace.
      ///
      /// URL: https://huggingface.co/{repo_id}/resolve/{revision}/{path}
      /// For datasets: https://huggingface.co/datasets/{repo_id}/resolve/{revision}/{path}
      /// For spaces: https://huggingface.co/spaces/{repo_id}/resolve/{revision}/{path}
      ///
      /// Returns the response as a byte stream + content_length.
      pub async fn download_file_stream(
          client: &Client,
          repo: &RepoRef,
          file_path: &str,
          token: Option<&str>,
      ) -> Result<(impl Stream<Item = Result<Bytes, reqwest::Error>>, Option<u64>), Hfs3Error> {
          let type_prefix = match repo.repo_type {
              RepoType::Model => "",            // models have no prefix
              RepoType::Dataset => "datasets/",
              RepoType::Space => "spaces/",
          };
          let url = format!(
              "https://huggingface.co/{}{}/resolve/{}/{}",
              type_prefix, repo.repo_id, repo.revision, file_path
          );

          let mut req = client.get(&url);
          if let Some(t) = token {
              req = req.bearer_auth(t);
          }

          let resp = req.send().await.map_err(Hfs3Error::Http)?;
          if !resp.status().is_success() {
              return Err(Hfs3Error::HfApi(format!(
                  "HF download returned {} for {}",
                  resp.status(), url
              )));
          }

          let content_length = resp.content_length();
          Ok((resp.bytes_stream(), content_length))
      }
      ```

      ## Tests

      Unit tests should NOT call the real HF API. Test URL construction and
      response parsing logic. Use #[cfg(test)] mod tests.

      ```rust
      #[cfg(test)]
      mod tests {
          use super::*;

          #[test]
          fn test_api_url_model() {
              // Verify URL construction for model repos
              let repo = RepoRef {
                  repo_id: "meta-llama/Llama-2-7b".into(),
                  repo_type: RepoType::Model,
                  revision: "main".into(),
              };
              let url = format!(
                  "https://huggingface.co/api/models/{}/tree/{}",
                  repo.repo_id, repo.revision
              );
              assert_eq!(url, "https://huggingface.co/api/models/meta-llama/Llama-2-7b/tree/main");
          }

          #[test]
          fn test_api_url_dataset() {
              let repo = RepoRef {
                  repo_id: "user/my-ds".into(),
                  repo_type: RepoType::Dataset,
                  revision: "main".into(),
              };
              let url = format!(
                  "https://huggingface.co/api/datasets/{}/tree/{}",
                  repo.repo_id, repo.revision
              );
              assert_eq!(url, "https://huggingface.co/api/datasets/user/my-ds/tree/main");
          }

          #[test]
          fn test_download_url_model() {
              let repo = RepoRef {
                  repo_id: "org/model".into(),
                  repo_type: RepoType::Model,
                  revision: "main".into(),
              };
              // Models: no type prefix in resolve URL
              let url = format!(
                  "https://huggingface.co/{}/resolve/{}/config.json",
                  repo.repo_id, repo.revision
              );
              assert_eq!(url, "https://huggingface.co/org/model/resolve/main/config.json");
          }

          #[test]
          fn test_download_url_space() {
              let repo = RepoRef {
                  repo_id: "user/app".into(),
                  repo_type: RepoType::Space,
                  revision: "main".into(),
              };
              let url = format!(
                  "https://huggingface.co/spaces/{}/resolve/{}/app.py",
                  repo.repo_id, repo.revision
              );
              assert_eq!(url, "https://huggingface.co/spaces/user/app/resolve/main/app.py");
          }
      }
      ```

  - id: A4
    name: s3_client
    level: 1
    depends_on: [A1]
    input_protos: [TypesOutput]
    output_protos: [S3ClientOutput]
    reads: [src/types.rs, src/error.rs]
    writes:
      - src/s3.rs
    test_glob: "s3::tests"
    completion_criteria:
      - "S3Ops struct wrapping aws_sdk_s3::Client"
      - "S3Ops::new(config) creates client from AppConfig (respects AWS_REGION)"
      - "chunk_size_for_file(file_size_bytes) -> usize returns 8MB for <1GB, 64MB for <5GB, 128MB for >=5GB"
      - "upload_multipart_stream(bucket, key, stream, file_size) streams chunks to S3 multipart upload"
      - "Multipart: create_multipart_upload -> upload_part per chunk -> complete_multipart_upload"
      - "For small files (<8MB), uses put_object instead of multipart"
      - "download_to_file(bucket, key, dest_path) downloads object to local file"
      - "list_objects(bucket, prefix) returns all keys under prefix with pagination"
      - "download_all(bucket, prefix, dest_dir) downloads all objects to local directory tree"
      - "Unit tests for chunk_size_for_file cover all three thresholds"
    agent_prompt: |
      You are implementing the S3 client module for hfs3.

      ## Dependencies
      Read src/types.rs for type definitions.
      Read src/error.rs for Hfs3Error.

      ## File to write

      ### src/s3.rs

      ```rust
      use aws_sdk_s3::Client as S3Client;
      use aws_sdk_s3::primitives::ByteStream;
      use bytes::Bytes;
      use futures::Stream;
      use std::path::Path;
      use crate::error::Hfs3Error;

      const MB: usize = 1024 * 1024;
      const GB: u64 = 1024 * 1024 * 1024;

      /// S3 operations wrapper.
      pub struct S3Ops {
          client: S3Client,
      }

      impl S3Ops {
          /// Create from AWS SDK config.
          pub async fn new(region: Option<&str>) -> Result<Self, Hfs3Error> {
              let mut config_loader = aws_config::defaults(aws_config::BehaviorVersion::latest());
              if let Some(r) = region {
                  config_loader = config_loader.region(aws_config::Region::new(r.to_string()));
              }
              let config = config_loader.load().await;
              Ok(Self {
                  client: S3Client::new(&config),
              })
          }

          /// From an existing SDK client (for testing).
          pub fn from_client(client: S3Client) -> Self {
              Self { client }
          }
      }

      /// Adaptive chunk size based on file size.
      ///   < 1 GB  ->  8 MB chunks
      ///   < 5 GB  -> 64 MB chunks
      ///   >= 5 GB -> 128 MB chunks
      pub fn chunk_size_for_file(file_size: u64) -> usize {
          if file_size < GB {
              8 * MB
          } else if file_size < 5 * GB {
              64 * MB
          } else {
              128 * MB
          }
      }
      ```

      Implement these methods on S3Ops:

      ### upload_multipart_stream
      - Takes: bucket, key, a Stream<Item=Result<Bytes, E>>, file_size: u64
      - For files < 8MB: collect all bytes, use put_object
      - For larger files:
        1. create_multipart_upload
        2. Buffer incoming stream chunks into part-sized buffers (chunk_size_for_file)
        3. upload_part for each buffer (parts are numbered 1, 2, 3, ...)
        4. complete_multipart_upload with all ETags
      - Returns: total bytes uploaded
      - On any part failure, abort_multipart_upload and return error
      - Log (tracing::info!) each part upload: "Uploaded part {n}/{est_total} ({size} bytes)"

      ### download_to_file
      - Takes: bucket, key, dest: &Path
      - get_object, write body to file
      - Create parent directories
      - Returns: bytes downloaded

      ### list_objects
      - Takes: bucket, prefix
      - Use list_objects_v2 with pagination (continuation_token)
      - Returns: Vec<(String, u64)> of (key, size) pairs
      - Filter out keys that are just the prefix itself

      ### download_all
      - Takes: bucket, prefix, dest_dir: &Path
      - list_objects, then download_to_file for each
      - Strip prefix from keys to build local paths
      - Returns: (files_count, total_bytes)

      ## Tests

      ```rust
      #[cfg(test)]
      mod tests {
          use super::*;

          #[test]
          fn test_chunk_size_small_file() {
              // 500 MB file -> 8 MB chunks
              assert_eq!(chunk_size_for_file(500 * 1024 * 1024), 8 * MB);
          }

          #[test]
          fn test_chunk_size_medium_file() {
              // 2 GB file -> 64 MB chunks
              assert_eq!(chunk_size_for_file(2 * GB), 64 * MB);
          }

          #[test]
          fn test_chunk_size_large_file() {
              // 10 GB file -> 128 MB chunks
              assert_eq!(chunk_size_for_file(10 * GB), 128 * MB);
          }

          #[test]
          fn test_chunk_size_boundary_1gb() {
              // Exactly 1 GB -> 64 MB (>= 1GB threshold)
              assert_eq!(chunk_size_for_file(GB), 64 * MB);
          }

          #[test]
          fn test_chunk_size_boundary_5gb() {
              // Exactly 5 GB -> 128 MB (>= 5GB threshold)
              assert_eq!(chunk_size_for_file(5 * GB), 128 * MB);
          }

          #[test]
          fn test_chunk_size_tiny_file() {
              // 1 KB file -> 8 MB (minimum chunk, will use put_object path)
              assert_eq!(chunk_size_for_file(1024), 8 * MB);
          }
      }
      ```

      NOTE: S3 upload/download methods are tested in integration tests (A9),
      not here. The unit tests focus on pure computation (chunk sizing).

  - id: A5
    name: concurrency
    level: 0
    depends_on: []
    input_protos: []
    output_protos: [ConcurrencyOutput]
    reads: []
    writes:
      - src/concurrency.rs
    test_glob: "concurrency::tests"
    completion_criteria:
      - "available_memory_bytes() reads /proc/meminfo MemAvailable line, parses kB value"
      - "Returns Err on non-Linux (no /proc/meminfo)"
      - "calculate_max_concurrency(chunk_size_bytes, available_mem_bytes) -> usize"
      - "Formula: available_mem / (chunk_size * 2), floor at 2, cap at 32"
      - "Assumes 2 chunks in flight per concurrent transfer"
      - "TransferPlan struct with max_concurrent: usize, chunk_size: usize, available_memory: u64"
      - "plan_transfer(files: &[(String, u64)]) -> TransferPlan computes plan from file sizes and available RAM"
      - "Logging: tracing::info! shows available RAM, chunk size, calculated max concurrency"
      - "All computation is pure and testable without /proc/meminfo"
    agent_prompt: |
      You are implementing the memory-aware concurrency module for hfs3.

      ## File to write

      ### src/concurrency.rs

      This module has NO dependencies on other hfs3 modules. It is pure computation
      plus one syscall (/proc/meminfo).

      ```rust
      use crate::error::Hfs3Error;

      /// Transfer plan computed from file sizes and available system memory.
      #[derive(Debug, Clone)]
      pub struct TransferPlan {
          pub max_concurrent: usize,  // max files to transfer in parallel
          pub chunk_size: usize,       // bytes per S3 multipart part
          pub available_memory: u64,   // bytes available from /proc/meminfo
      }

      /// Read MemAvailable from /proc/meminfo.
      /// Returns bytes (the file reports kB, multiply by 1024).
      pub fn available_memory_bytes() -> Result<u64, Hfs3Error> {
          let contents = std::fs::read_to_string("/proc/meminfo")
              .map_err(|e| Hfs3Error::Io(e))?;

          for line in contents.lines() {
              if line.starts_with("MemAvailable:") {
                  // Format: "MemAvailable:   12345678 kB"
                  let parts: Vec<&str> = line.split_whitespace().collect();
                  if parts.len() >= 2 {
                      let kb: u64 = parts[1].parse()
                          .map_err(|_| Hfs3Error::Parse("Failed to parse MemAvailable".into()))?;
                      return Ok(kb * 1024);
                  }
              }
          }
          Err(Hfs3Error::Parse("MemAvailable not found in /proc/meminfo".into()))
      }

      /// Calculate max concurrent file transfers.
      ///
      /// Assumes each concurrent transfer buffers ~2 chunks in flight
      /// (one being uploaded, one being filled from download).
      ///
      /// Formula: available_memory / (chunk_size * 2)
      /// Floor: 2 (always allow at least 2 concurrent transfers)
      /// Cap: 32 (avoid overwhelming the network/S3)
      pub fn calculate_max_concurrency(chunk_size: usize, available_memory: u64) -> usize {
          let bytes_per_transfer = (chunk_size as u64) * 2;
          if bytes_per_transfer == 0 {
              return 2;
          }
          let calculated = (available_memory / bytes_per_transfer) as usize;
          calculated.clamp(2, 32)
      }

      /// Compute a transfer plan from file list and system memory.
      ///
      /// Uses the largest file to determine chunk size (conservative),
      /// then computes concurrency from available RAM.
      pub fn plan_transfer(files: &[(&str, u64)]) -> Result<TransferPlan, Hfs3Error> {
          let available = available_memory_bytes()?;
          let max_file_size = files.iter().map(|(_, s)| *s).max().unwrap_or(0);
          let chunk_size = crate::s3::chunk_size_for_file(max_file_size);
          let max_concurrent = calculate_max_concurrency(chunk_size, available);

          tracing::info!(
              available_memory_mb = available / (1024 * 1024),
              chunk_size_mb = chunk_size / (1024 * 1024),
              max_concurrent,
              file_count = files.len(),
              largest_file_mb = max_file_size / (1024 * 1024),
              "Transfer plan computed"
          );

          Ok(TransferPlan {
              max_concurrent,
              chunk_size,
              available_memory: available,
          })
      }

      /// Same as plan_transfer but accepts pre-computed available memory.
      /// Used in tests and when /proc/meminfo is unavailable.
      pub fn plan_transfer_with_memory(
          files: &[(&str, u64)],
          available_memory: u64,
      ) -> TransferPlan {
          let max_file_size = files.iter().map(|(_, s)| *s).max().unwrap_or(0);
          let chunk_size = crate::s3::chunk_size_for_file(max_file_size);
          let max_concurrent = calculate_max_concurrency(chunk_size, available_memory);

          tracing::info!(
              available_memory_mb = available_memory / (1024 * 1024),
              chunk_size_mb = chunk_size / (1024 * 1024),
              max_concurrent,
              file_count = files.len(),
              largest_file_mb = max_file_size / (1024 * 1024),
              "Transfer plan computed"
          );

          TransferPlan {
              max_concurrent,
              chunk_size,
              available_memory,
          }
      }
      ```

      ## Important design note

      `plan_transfer` calls `crate::s3::chunk_size_for_file` — but A5 runs at
      level 0, and A4 (s3_client) also runs at level 0. This is a SOFT dependency:
      `chunk_size_for_file` is a pure function with no state. The concurrency module
      can compile with a stub of s3.rs that just has the chunk_size_for_file function
      (which A0/scaffold creates). In testing, use `plan_transfer_with_memory` which
      avoids the /proc/meminfo call, and test `calculate_max_concurrency` directly
      which has no external deps.

      If this causes a compile issue, MOVE `chunk_size_for_file` into this module
      or into types.rs and re-export from s3.rs. The function is pure math —
      it belongs wherever it compiles first.

      ## Tests

      ```rust
      #[cfg(test)]
      mod tests {
          use super::*;

          #[test]
          fn test_calculate_max_concurrency_normal() {
              // 16 GB available, 8 MB chunks
              // 16*1024*1024*1024 / (8*1024*1024 * 2) = 1024 -> capped at 32
              let result = calculate_max_concurrency(8 * 1024 * 1024, 16 * 1024 * 1024 * 1024);
              assert_eq!(result, 32);
          }

          #[test]
          fn test_calculate_max_concurrency_low_memory() {
              // 100 MB available, 64 MB chunks
              // 100*1024*1024 / (64*1024*1024 * 2) = 0 -> floored at 2
              let result = calculate_max_concurrency(64 * 1024 * 1024, 100 * 1024 * 1024);
              assert_eq!(result, 2);
          }

          #[test]
          fn test_calculate_max_concurrency_moderate() {
              // 1 GB available, 8 MB chunks
              // 1024*1024*1024 / (8*1024*1024 * 2) = 64 -> capped at 32
              let result = calculate_max_concurrency(8 * 1024 * 1024, 1024 * 1024 * 1024);
              assert_eq!(result, 32);
          }

          #[test]
          fn test_calculate_max_concurrency_small_memory() {
              // 50 MB available, 8 MB chunks
              // 50*1024*1024 / (8*1024*1024 * 2) = 3
              let result = calculate_max_concurrency(8 * 1024 * 1024, 50 * 1024 * 1024);
              assert_eq!(result, 3);
          }

          #[test]
          fn test_calculate_max_concurrency_floor() {
              // 1 MB available, 8 MB chunks -> 0, floored to 2
              let result = calculate_max_concurrency(8 * 1024 * 1024, 1024 * 1024);
              assert_eq!(result, 2);
          }

          #[test]
          fn test_calculate_max_concurrency_zero_chunk() {
              // Edge case: 0 chunk size -> floor at 2
              let result = calculate_max_concurrency(0, 1024 * 1024 * 1024);
              assert_eq!(result, 2);
          }

          #[test]
          fn test_plan_transfer_with_memory() {
              let files = vec![
                  ("small.txt", 1024u64),
                  ("big.bin", 500 * 1024 * 1024),  // 500 MB -> 8 MB chunks
              ];
              let plan = plan_transfer_with_memory(&files, 1024 * 1024 * 1024); // 1 GB
              assert_eq!(plan.chunk_size, 8 * 1024 * 1024);
              assert!(plan.max_concurrent >= 2);
              assert!(plan.max_concurrent <= 32);
          }

          #[test]
          fn test_plan_transfer_with_large_files() {
              let files = vec![
                  ("huge.safetensors", 6 * 1024 * 1024 * 1024u64), // 6 GB -> 128 MB chunks
              ];
              let plan = plan_transfer_with_memory(&files, 32 * 1024 * 1024 * 1024); // 32 GB
              assert_eq!(plan.chunk_size, 128 * 1024 * 1024);
          }

          #[cfg(target_os = "linux")]
          #[test]
          fn test_available_memory_bytes_reads_procfs() {
              // On Linux, /proc/meminfo should exist and return > 0
              let mem = available_memory_bytes().unwrap();
              assert!(mem > 0, "MemAvailable should be positive");
              // Sanity: should be less than 1 TB
              assert!(mem < 1024 * 1024 * 1024 * 1024);
          }
      }
      ```

  - id: A6
    name: pipeline
    level: 2
    depends_on: [A1, A3, A4, A5]
    input_protos: [TypesOutput, HfClientOutput, S3ClientOutput, ConcurrencyOutput]
    output_protos: [PipelineOutput]
    reads: [src/types.rs, src/error.rs, src/hf.rs, src/s3.rs, src/concurrency.rs, src/config.rs]
    writes:
      - src/pipeline.rs
    test_glob: "pipeline::tests"
    completion_criteria:
      - "mirror_repo(config, repo_ref) -> Result<MirrorResult> orchestrates the full HF-to-S3 pipeline"
      - "Steps: list files -> plan transfer -> stream each file from HF to S3 multipart"
      - "Uses tokio::sync::Semaphore for concurrency limiting"
      - "Progress logging to stderr: per-file start/complete, overall progress"
      - "pull_repo(config, repo_ref, dest) -> Result<PullResult> downloads from S3 to local"
      - "Both functions return result structs that serialize to JSON for stdout"
      - "Tracks total bytes transferred and duration"
      - "On individual file failure: log error, continue with remaining files, report partial success"
      - "Unit tests verify orchestration logic with mocked HF/S3 clients"
    agent_prompt: |
      You are implementing the streaming transfer pipeline for hfs3.

      ## Dependencies
      Read all files in src/ to understand the types and client APIs.

      ## File to write

      ### src/pipeline.rs

      This is the orchestration layer. It wires together hf, s3, and concurrency.

      ```rust
      use std::path::Path;
      use std::sync::Arc;
      use std::time::Instant;
      use tokio::sync::Semaphore;
      use crate::config::AppConfig;
      use crate::error::Hfs3Error;
      use crate::hf;
      use crate::s3::S3Ops;
      use crate::concurrency;
      use crate::types::{RepoRef, MirrorResult, PullResult};

      /// Stream an entire HF repo to S3.
      ///
      /// 1. List all files in the repo via HF REST API
      /// 2. Compute transfer plan (chunk size + concurrency from available memory)
      /// 3. For each file, stream from HF -> S3 multipart upload (zero-copy)
      /// 4. Use semaphore to limit concurrent transfers
      /// 5. Log progress to stderr, return MirrorResult for JSON output
      pub async fn mirror_repo(
          config: &AppConfig,
          repo: &RepoRef,
      ) -> Result<MirrorResult, Hfs3Error> {
          let start = Instant::now();
          let http_client = reqwest::Client::new();
          let s3_ops = S3Ops::new(config.aws_region.as_deref()).await?;

          // 1. List files
          eprintln!("Listing files in {}/{}...", repo.repo_type, repo.repo_id);
          let files = hf::list_repo_files(&http_client, repo, config.hf_token.as_deref()).await?;
          eprintln!("Found {} files", files.len());

          if files.is_empty() {
              return Ok(MirrorResult {
                  repo_id: repo.repo_id.clone(),
                  repo_type: repo.repo_type.to_string(),
                  bucket: config.s3_bucket.clone(),
                  prefix: config.s3_prefix_for(&repo.repo_type.to_string(), &repo.repo_id),
                  files_transferred: 0,
                  bytes_transferred: 0,
                  duration_secs: start.elapsed().as_secs_f64(),
              });
          }

          // 2. Compute transfer plan
          let file_specs: Vec<(&str, u64)> = files.iter()
              .map(|f| (f.path.as_str(), f.size))
              .collect();
          let plan = concurrency::plan_transfer(&file_specs)
              .unwrap_or_else(|_| {
                  // Fallback if /proc/meminfo unavailable
                  eprintln!("Warning: Could not read /proc/meminfo, using default concurrency=4");
                  concurrency::plan_transfer_with_memory(&file_specs, 2 * 1024 * 1024 * 1024)
              });

          eprintln!(
              "Transfer plan: {} concurrent, {} MB chunks, {} MB available RAM",
              plan.max_concurrent,
              plan.chunk_size / (1024 * 1024),
              plan.available_memory / (1024 * 1024),
          );

          // 3. Stream each file
          let s3_prefix = config.s3_prefix_for(&repo.repo_type.to_string(), &repo.repo_id);
          let semaphore = Arc::new(Semaphore::new(plan.max_concurrent));
          let s3_ops = Arc::new(s3_ops);
          let http_client = Arc::new(http_client);

          let mut handles = Vec::new();
          for file in &files {
              let sem = semaphore.clone();
              let s3 = s3_ops.clone();
              let client = http_client.clone();
              let bucket = config.s3_bucket.clone();
              let key = format!("{}/{}", s3_prefix, file.path);
              let file_path = file.path.clone();
              let file_size = file.size;
              let repo_clone = repo.clone();
              let token = config.hf_token.clone();

              let handle = tokio::spawn(async move {
                  let _permit = sem.acquire().await.unwrap();
                  eprintln!("  Streaming {} ({} bytes)...", file_path, file_size);

                  let (stream, _) = hf::download_file_stream(
                      &client, &repo_clone, &file_path, token.as_deref()
                  ).await?;

                  let bytes = s3.upload_multipart_stream(
                      &bucket, &key, stream, file_size
                  ).await?;

                  eprintln!("  Done: {} ({} bytes)", file_path, bytes);
                  Ok::<u64, Hfs3Error>(bytes)
              });
              handles.push(handle);
          }

          // Collect results
          let mut total_bytes = 0u64;
          let mut files_ok = 0usize;
          for handle in handles {
              match handle.await {
                  Ok(Ok(bytes)) => {
                      total_bytes += bytes;
                      files_ok += 1;
                  }
                  Ok(Err(e)) => {
                      eprintln!("  Error: {e}");
                  }
                  Err(e) => {
                      eprintln!("  Task panicked: {e}");
                  }
              }
          }

          Ok(MirrorResult {
              repo_id: repo.repo_id.clone(),
              repo_type: repo.repo_type.to_string(),
              bucket: config.s3_bucket.clone(),
              prefix: s3_prefix,
              files_transferred: files_ok,
              bytes_transferred: total_bytes,
              duration_secs: start.elapsed().as_secs_f64(),
          })
      }

      /// Download a mirrored repo from S3 to a local directory.
      pub async fn pull_repo(
          config: &AppConfig,
          repo: &RepoRef,
          dest: &Path,
      ) -> Result<PullResult, Hfs3Error> {
          let start = Instant::now();
          let s3_ops = S3Ops::new(config.aws_region.as_deref()).await?;
          let s3_prefix = config.s3_prefix_for(&repo.repo_type.to_string(), &repo.repo_id);

          eprintln!("Pulling s3://{}/{} to {}...", config.s3_bucket, s3_prefix, dest.display());
          let (files_downloaded, bytes_downloaded) = s3_ops
              .download_all(&config.s3_bucket, &s3_prefix, dest)
              .await?;

          eprintln!("Downloaded {} files ({} bytes)", files_downloaded, bytes_downloaded);

          Ok(PullResult {
              repo_id: repo.repo_id.clone(),
              repo_type: repo.repo_type.to_string(),
              dest: dest.to_path_buf(),
              files_downloaded,
              bytes_downloaded,
              duration_secs: start.elapsed().as_secs_f64(),
          })
      }
      ```

      ## Tests

      Test the orchestration logic. Since mirror_repo calls real HF/S3 APIs,
      unit tests should focus on:
      - Transfer plan computation edge cases
      - Graceful handling of empty file lists
      - S3 prefix construction

      ```rust
      #[cfg(test)]
      mod tests {
          use super::*;
          use crate::types::RepoType;

          #[test]
          fn test_mirror_empty_file_list_produces_zero_result() {
              // Verify the early-return logic for repos with no files
              // This tests the struct construction, not the async pipeline
              let result = MirrorResult {
                  repo_id: "user/repo".into(),
                  repo_type: "model".into(),
                  bucket: "bucket".into(),
                  prefix: "hfs3-mirror/model/user--repo".into(),
                  files_transferred: 0,
                  bytes_transferred: 0,
                  duration_secs: 0.0,
              };
              assert_eq!(result.files_transferred, 0);
              let json = serde_json::to_string(&result).unwrap();
              assert!(json.contains("\"files_transferred\":0"));
          }

          #[test]
          fn test_pull_result_serializes_to_json() {
              let result = PullResult {
                  repo_id: "user/repo".into(),
                  repo_type: "model".into(),
                  dest: std::path::PathBuf::from("./repo"),
                  files_downloaded: 5,
                  bytes_downloaded: 1024,
                  duration_secs: 1.5,
              };
              let json = serde_json::to_string_pretty(&result).unwrap();
              assert!(json.contains("\"files_downloaded\": 5"));
              assert!(json.contains("\"bytes_downloaded\": 1024"));
          }
      }
      ```

  - id: A7
    name: docker
    level: 0
    depends_on: []
    input_protos: []
    output_protos: [DockerOutput]
    reads: [tests/test_docker.py]
    writes:
      - src/docker.rs
    test_glob: "docker::tests"
    completion_criteria:
      - "build_image(context_dir, tag) -> Result<()> runs 'docker build -t {tag} .'"
      - "run_image(tag, port, extra_args) -> Result<()> runs 'docker run --rm -p {port}:{port} {tag}'"
      - "build_image returns error if Dockerfile not found (matches Python test)"
      - "Uses std::process::Command (synchronous, called from async context via spawn_blocking)"
      - "Logs docker commands to stderr before execution"
    agent_prompt: |
      You are implementing the Docker module for hfs3.

      ## Dependencies
      Read src/error.rs for Hfs3Error.
      Read tests/test_docker.py for the behavioral spec (missing Dockerfile raises error).

      ## File to write

      ### src/docker.rs

      ```rust
      use std::path::Path;
      use std::process::Command;
      use crate::error::Hfs3Error;

      /// Build a Docker image from a directory containing a Dockerfile.
      ///
      /// Equivalent to: docker build -t {tag} .
      /// Runs in the context_dir directory.
      pub async fn build_image(context_dir: &Path, tag: &str) -> Result<(), Hfs3Error> {
          // Check Dockerfile exists first
          if !context_dir.join("Dockerfile").exists() {
              return Err(Hfs3Error::Docker(
                  format!("No Dockerfile in {}", context_dir.display())
              ));
          }

          let dir = context_dir.to_path_buf();
          let tag = tag.to_string();

          eprintln!("Running: docker build -t {} .", tag);

          // Use spawn_blocking since Command::output() is blocking
          let output = tokio::task::spawn_blocking(move || {
              Command::new("docker")
                  .args(["build", "-t", &tag, "."])
                  .current_dir(&dir)
                  .output()
          })
          .await
          .map_err(|e| Hfs3Error::Docker(format!("spawn error: {e}")))?
          .map_err(|e| Hfs3Error::Docker(format!("docker build failed: {e}")))?;

          if !output.status.success() {
              let stderr = String::from_utf8_lossy(&output.stderr);
              return Err(Hfs3Error::Docker(format!("docker build failed: {stderr}")));
          }

          Ok(())
      }

      /// Run a Docker image, mapping a port to localhost.
      ///
      /// Equivalent to: docker run --rm -p {port}:{port} [extra_args...] {tag}
      pub async fn run_image(
          tag: &str,
          port: u16,
          extra_args: Option<&[&str]>,
      ) -> Result<(), Hfs3Error> {
          let mut cmd_args = vec![
              "run".to_string(),
              "--rm".to_string(),
              "-p".to_string(),
              format!("{port}:{port}"),
          ];
          if let Some(extras) = extra_args {
              cmd_args.extend(extras.iter().map(|s| s.to_string()));
          }
          cmd_args.push(tag.to_string());

          eprintln!("Running: docker {}", cmd_args.join(" "));

          let output = tokio::task::spawn_blocking(move || {
              Command::new("docker")
                  .args(&cmd_args)
                  .status()
          })
          .await
          .map_err(|e| Hfs3Error::Docker(format!("spawn error: {e}")))?
          .map_err(|e| Hfs3Error::Docker(format!("docker run failed: {e}")))?;

          if !output.success() {
              return Err(Hfs3Error::Docker("docker run exited with non-zero status".into()));
          }

          Ok(())
      }
      ```

      ## Tests

      ```rust
      #[cfg(test)]
      mod tests {
          use super::*;
          use tempfile::tempdir;

          #[tokio::test]
          async fn test_missing_dockerfile_raises() {
              let dir = tempdir().unwrap();
              let result = build_image(dir.path(), "test-tag").await;
              assert!(result.is_err());
              let err_msg = result.unwrap_err().to_string();
              assert!(err_msg.contains("No Dockerfile"), "Error should mention missing Dockerfile: {err_msg}");
          }

          #[tokio::test]
          async fn test_missing_dockerfile_includes_path() {
              let dir = tempdir().unwrap();
              let result = build_image(dir.path(), "test-tag").await;
              let err_msg = result.unwrap_err().to_string();
              assert!(err_msg.contains(dir.path().to_str().unwrap()));
          }
      }
      ```

      NOTE: Add `tempfile = "3"` to [dev-dependencies] in Cargo.toml if the
      scaffold agent hasn't already. If you can't modify Cargo.toml (it's owned
      by A0), use std::env::temp_dir() and manual cleanup instead.

      IMPORTANT: Since A0 owns Cargo.toml, and you need tempfile as a dev-dep,
      either: (a) A0's scaffold already includes it, or (b) you skip tempfile
      and use std::env::temp_dir(). Check Cargo.toml first. If tempfile is
      missing, add a comment noting it should be added, and use std alternatives.

  - id: A8
    name: cli_wiring
    level: 3
    depends_on: [A0, A1, A2, A6, A7]
    input_protos: [ScaffoldOutput, TypesOutput, ConfigOutput, PipelineOutput, DockerOutput]
    output_protos: [CliOutput]
    reads: [src/types.rs, src/config.rs, src/pipeline.rs, src/docker.rs, src/error.rs]
    writes:
      - src/cli.rs
    test_glob: none
    completion_criteria:
      - "cli.rs contains clap structs and run() function that A0's main.rs calls"
      - "Initializes tracing-subscriber with stderr output and env filter"
      - "mirror command: parse repo -> mirror_repo -> print JSON to stdout"
      - "pull command: parse repo -> pull_repo -> print JSON to stdout"
      - "run command: parse repo -> pull if needed -> build -> run -> print JSON to stdout"
      - "All errors propagated via Hfs3Error, main.rs handles exit code"
      - "JSON summary always goes to stdout (even on partial failure)"
      - "Compiles and runs: cargo run -- --help shows all subcommands"
    agent_prompt: |
      You are wiring the CLI module for hfs3.

      ## Dependencies
      Read all src/*.rs files to understand the module APIs.

      ## File to write

      ### src/cli.rs

      This replaces the stub created by A0. The main.rs entrypoint calls
      `hfs3::cli::run().await` — this module contains all clap definitions
      and command handler logic.

      ```rust
      use clap::{Parser, Subcommand};
      use std::path::PathBuf;
      use crate::error::Hfs3Error;

      #[derive(Parser)]
      #[command(name = "hfs3", about = "Mirror HuggingFace repos to S3")]
      struct Cli {
          #[command(subcommand)]
          command: Commands,
      }

      #[derive(Subcommand)]
      enum Commands {
          /// Stream HuggingFace repo to S3
          Mirror {
              /// HF repo ID or URL (e.g. "meta-llama/Llama-2-7b")
              repo: String,
          },
          /// Download from S3 to local directory
          Pull {
              /// HF repo ID or URL
              repo: String,
              /// Local destination directory
              #[arg(long, default_value = "./repo")]
              dest: PathBuf,
          },
          /// Pull from S3, build and run Docker image
          Run {
              /// HF repo ID or URL
              repo: String,
              /// Local destination directory
              #[arg(long, default_value = "./repo")]
              dest: PathBuf,
              /// Port to expose
              #[arg(long, default_value_t = 7860)]
              port: u16,
              /// Re-download from S3 even if local dir exists
              #[arg(long)]
              force: bool,
          },
      }

      /// CLI entrypoint. Called from main.rs.
      pub async fn run() -> Result<(), Box<dyn std::error::Error>> {
          // Init tracing: human-readable to stderr, controlled by RUST_LOG
          tracing_subscriber::fmt()
              .with_env_filter(
                  tracing_subscriber::EnvFilter::try_from_default_env()
                      .unwrap_or_else(|_| "hfs3=info".parse().unwrap())
              )
              .with_writer(std::io::stderr)
              .init();

          let cli = Cli::parse();

          match cli.command {
              Commands::Mirror { repo } => cmd_mirror(&repo).await?,
              Commands::Pull { repo, dest } => cmd_pull(&repo, &dest).await?,
              Commands::Run { repo, dest, port, force } => {
                  cmd_run(&repo, &dest, port, force).await?
              }
          };

          Ok(())
      }

      async fn cmd_mirror(repo_str: &str) -> Result<(), Hfs3Error> {
          let config = crate::config::AppConfig::from_env()?;
          let repo = crate::types::parse_repo_url(repo_str)?;

          let result = crate::pipeline::mirror_repo(&config, &repo).await?;

          // JSON summary to stdout
          println!("{}", serde_json::to_string_pretty(&result).unwrap());
          Ok(())
      }

      async fn cmd_pull(repo_str: &str, dest: &PathBuf) -> Result<(), Hfs3Error> {
          let config = crate::config::AppConfig::from_env()?;
          let repo = crate::types::parse_repo_url(repo_str)?;

          let result = crate::pipeline::pull_repo(&config, &repo, dest).await?;

          println!("{}", serde_json::to_string_pretty(&result).unwrap());
          Ok(())
      }

      async fn cmd_run(
          repo_str: &str,
          dest: &PathBuf,
          port: u16,
          force: bool,
      ) -> Result<(), Hfs3Error> {
          let config = crate::config::AppConfig::from_env()?;
          let repo = crate::types::parse_repo_url(repo_str)?;

          // Pull if dir doesn't exist or --force
          if !dest.exists() || force {
              let pull_result = crate::pipeline::pull_repo(&config, &repo, dest).await?;
              eprintln!("Pulled {} files", pull_result.files_downloaded);
          }

          // Build
          let tag = format!("hfs3/{}", repo.repo_id).to_lowercase();
          eprintln!("Building Docker image {tag}...");
          crate::docker::build_image(dest, &tag).await?;

          // Run
          eprintln!("Running on port {port}...");
          crate::docker::run_image(&tag, port, None).await?;

          // JSON summary
          let result = crate::types::RunResult {
              repo_id: repo.repo_id,
              repo_type: repo.repo_type.to_string(),
              image_tag: tag,
              port,
          };
          println!("{}", serde_json::to_string_pretty(&result).unwrap());
          Ok(())
      }
      ```

      ## Validation
      - `cargo build` succeeds
      - `cargo run -- --help` shows mirror, pull, run subcommands
      - `cargo run -- mirror --help` shows repo argument
      - `cargo run -- pull --help` shows repo and --dest
      - `cargo run -- run --help` shows repo, --dest, --port, --force

  - id: A9
    name: integration_test
    level: 4
    depends_on: [A8]
    input_protos: [CliOutput]
    output_protos: [IntegrationTestOutput]
    reads: [src/main.rs, src/types.rs, src/hf.rs]
    writes:
      - tests/integration_test.rs
    test_glob: "integration_test"
    completion_criteria:
      - "Test downloads file listing from a real tiny public HF repo"
      - "Uses hf-internal-testing/tiny-random-bert (or similar tiny public repo, <100KB total)"
      - "Verifies list_repo_files returns non-empty Vec<HfFileEntry>"
      - "Verifies each file has non-empty path and oid"
      - "Verifies download_file_stream returns bytes for at least one file"
      - "Test is #[ignore] by default (requires network), run with just test-integration"
      - "Does NOT test S3 (no mock S3 in Rust tests)"
    agent_prompt: |
      You are writing the integration test for hfs3.

      ## Dependencies
      Read src/hf.rs and src/types.rs for the API.

      ## File to write

      ### tests/integration_test.rs

      This is a Rust integration test file (in tests/ directory, outside src/).
      It tests real HTTP calls to the HuggingFace API.

      ```rust
      //! Integration tests for hfs3 HuggingFace API client.
      //!
      //! These tests hit the real HF API and require network access.
      //! Run with: cargo test --test integration_test -- --ignored
      //! Or:       just test-integration

      use hfs3::hf;
      use hfs3::types::{RepoRef, RepoType};

      /// Test repo: tiny public model with known small files.
      /// hf-internal-testing/tiny-random-bert is ~100KB total.
      const TEST_REPO: &str = "hf-internal-testing/tiny-random-bert";

      #[tokio::test]
      #[ignore] // requires network
      async fn test_list_repo_files_returns_files() {
          let client = reqwest::Client::new();
          let repo = RepoRef {
              repo_id: TEST_REPO.into(),
              repo_type: RepoType::Model,
              revision: "main".into(),
          };

          let files = hf::list_repo_files(&client, &repo, None).await.unwrap();

          assert!(!files.is_empty(), "Expected at least one file in {TEST_REPO}");

          // Should have config.json at minimum
          let has_config = files.iter().any(|f| f.path == "config.json");
          assert!(has_config, "Expected config.json in file listing");

          // All files should have non-empty paths
          for file in &files {
              assert!(!file.path.is_empty(), "File path should not be empty");
          }
      }

      #[tokio::test]
      #[ignore] // requires network
      async fn test_download_file_stream_returns_bytes() {
          let client = reqwest::Client::new();
          let repo = RepoRef {
              repo_id: TEST_REPO.into(),
              repo_type: RepoType::Model,
              revision: "main".into(),
          };

          // Download config.json — should be small JSON
          let (stream, content_length) = hf::download_file_stream(
              &client, &repo, "config.json", None
          ).await.unwrap();

          // Collect the stream
          use futures::StreamExt;
          let mut bytes = Vec::new();
          let mut stream = Box::pin(stream);
          while let Some(chunk) = stream.next().await {
              bytes.extend_from_slice(&chunk.unwrap());
          }

          assert!(!bytes.is_empty(), "Downloaded bytes should not be empty");

          // Should be valid JSON
          let parsed: serde_json::Value = serde_json::from_slice(&bytes).unwrap();
          assert!(parsed.is_object(), "config.json should be a JSON object");
      }

      #[tokio::test]
      #[ignore] // requires network
      async fn test_list_files_dataset() {
          let client = reqwest::Client::new();
          // Use a tiny public dataset
          let repo = RepoRef {
              repo_id: "hf-internal-testing/fixtures_ade20k".into(),
              repo_type: RepoType::Dataset,
              revision: "main".into(),
          };

          let result = hf::list_repo_files(&client, &repo, None).await;
          // May succeed or fail depending on dataset availability
          // The test verifies the API call path is correct
          if let Ok(files) = result {
              assert!(!files.is_empty());
          }
      }

      #[tokio::test]
      #[ignore]
      async fn test_parse_and_list_from_url() {
          let client = reqwest::Client::new();
          let repo = hfs3::types::parse_repo_url(
              "https://huggingface.co/hf-internal-testing/tiny-random-bert"
          ).unwrap();

          assert_eq!(repo.repo_type, RepoType::Model);

          let files = hf::list_repo_files(&client, &repo, None).await.unwrap();
          assert!(!files.is_empty());
      }
      ```

      ## Also update justfile

      Add this recipe (you need to coordinate with A0 — if you can't modify
      justfile, document this recipe in a comment at the top of the test file):

      ```just
      test-integration:
          cargo test --test integration_test -- --ignored --nocapture
      ```

      Since A0 owns justfile, add a NOTE comment in the test file:
      ```rust
      // NOTE: Run integration tests with:
      //   cargo test --test integration_test -- --ignored --nocapture
      // Or add to justfile:
      //   test-integration:
      //       cargo test --test integration_test -- --ignored --nocapture
      ```

  - id: A10
    name: ci
    level: 1
    depends_on: [A0]
    input_protos: [ScaffoldOutput]
    output_protos: [CiOutput]
    reads: [Cargo.toml, justfile]
    writes:
      - .github/workflows/ci.yml
      - .github/workflows/release.yml
    test_glob: none
    completion_criteria:
      - "ci.yml runs on push to main and PRs"
      - "ci.yml steps: checkout, install Rust stable, cargo fmt check, cargo clippy, cargo test"
      - "release.yml runs on tag push (v*)"
      - "release.yml builds linux-x86_64 and linux-aarch64 binaries"
      - "Uses cross-compilation via cross or cargo-zigbuild for aarch64"
      - "Uploads binaries to GitHub Releases"
      - "Binary names: hfs3-linux-x86_64, hfs3-linux-aarch64"
    agent_prompt: |
      You are creating the GitHub Actions CI/CD workflows for hfs3.

      ## Files to write

      ### .github/workflows/ci.yml

      ```yaml
      name: CI

      on:
        push:
          branches: [main]
        pull_request:
          branches: [main]

      env:
        CARGO_TERM_COLOR: always
        RUSTFLAGS: "-D warnings"

      jobs:
        check:
          name: Check & Test
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4

            - name: Install Rust
              uses: dtolnay/rust-toolchain@stable
              with:
                components: rustfmt, clippy

            - name: Cache cargo
              uses: actions/cache@v4
              with:
                path: |
                  ~/.cargo/registry
                  ~/.cargo/git
                  target
                key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
                restore-keys: ${{ runner.os }}-cargo-

            - name: Format check
              run: cargo fmt -- --check

            - name: Clippy
              run: cargo clippy -- -D warnings

            - name: Test
              run: cargo test

            - name: Build release
              run: cargo build --release
      ```

      ### .github/workflows/release.yml

      ```yaml
      name: Release

      on:
        push:
          tags: ["v*"]

      permissions:
        contents: write

      jobs:
        build:
          name: Build ${{ matrix.target }}
          runs-on: ubuntu-latest
          strategy:
            matrix:
              include:
                - target: x86_64-unknown-linux-gnu
                  artifact: hfs3-linux-x86_64
                - target: aarch64-unknown-linux-gnu
                  artifact: hfs3-linux-aarch64

          steps:
            - uses: actions/checkout@v4

            - name: Install Rust
              uses: dtolnay/rust-toolchain@stable
              with:
                targets: ${{ matrix.target }}

            - name: Install cross
              run: cargo install cross --git https://github.com/cross-rs/cross

            - name: Build
              run: cross build --release --target ${{ matrix.target }}

            - name: Rename binary
              run: |
                cp target/${{ matrix.target }}/release/hfs3 ${{ matrix.artifact }}

            - name: Upload artifact
              uses: actions/upload-artifact@v4
              with:
                name: ${{ matrix.artifact }}
                path: ${{ matrix.artifact }}

        release:
          name: Create Release
          needs: build
          runs-on: ubuntu-latest
          steps:
            - name: Download artifacts
              uses: actions/download-artifact@v4

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                files: |
                  hfs3-linux-x86_64/hfs3-linux-x86_64
                  hfs3-linux-aarch64/hfs3-linux-aarch64
                generate_release_notes: true
      ```

      ## Validation
      - YAML is valid (no syntax errors)
      - ci.yml triggers on push to main and PRs
      - release.yml triggers on v* tags only
      - Both x86_64 and aarch64 targets present

# ============================================================
# Dispatch Table
# ============================================================
dispatch:
  - level: 0
    agents: [A0, A1, A2, A5, A7]
    parallelism: 5
    blocked_by: nothing
    gate: "cargo build && cargo test -- --skip integration"

  - level: 1
    agents: [A3, A4, A10]
    parallelism: 3
    blocked_by: [A0, A1]
    gate: "cargo build && cargo test -- --skip integration"

  - level: 2
    agents: [A6]
    parallelism: 1
    blocked_by: [A1, A3, A4, A5]
    gate: "cargo build && cargo test -- --skip integration"

  - level: 3
    agents: [A8]
    parallelism: 1
    blocked_by: [A0, A1, A2, A6, A7]
    gate: "cargo build && cargo run -- --help"

  - level: 4
    agents: [A9]
    parallelism: 1
    blocked_by: [A8]
    gate: "cargo test --test integration_test -- --ignored"

# ============================================================
# Critical Path
# ============================================================
# A0 (scaffold) -> A3 (hf_client) -> A6 (pipeline) -> A8 (cli_wiring) -> A9 (integration_test)
# Estimated: ~6-8 hours total, ~3-4 hours on critical path

# ============================================================
# Coordinator Protocol
# ============================================================
coordinator:
  bootstrap:
    - "Read spec/plan.yaml"
    - "Read spec/contracts/agent_io.proto"
    - "Check which agent output files already exist"
    - "Determine next incomplete level"

  dispatch:
    - "For each agent in the current level, launch a worker agent"
    - "Workers run in parallel within a level"
    - "Each worker reads its agent_prompt and executes independently"

  gate:
    - "After all agents in a level complete, run the gate command"
    - "Gate must pass before advancing to next level"
    - "If gate fails, identify which agent's output is broken"

  proto_validation:
    - "At each level boundary, verify output files exist for all completed agents"
    - "Verify public API surface matches output_protos"
    - "Mismatch = hard failure, do not advance"

# ============================================================
# Hard Rules
# ============================================================
hard_rules:
  - "No agent modifies files outside its writes list"
  - "Tests are the only completion signal — not compilation, not self-report"
  - "Proto contracts in spec/contracts/ are never modified by worker agents"
  - "The coordinator never writes application code"
  - "File ownership is exclusive — no file appears in two agents' writes"
  - "All commands go through just (justfile is the standard interface)"
  - "Human progress to stderr, JSON summary to stdout"
  - "No unsafe Rust unless absolutely necessary (document why)"
  - "All public functions have doc comments"
  - "Error handling: use Hfs3Error variants, no unwrap() in non-test code"

# ============================================================
# Post-Completion
# ============================================================
post_completion:
  - "Run full test suite: just test"
  - "Run integration test: just test-integration (requires network)"
  - "Run clippy: just clippy"
  - "Run fmt check: just fmt-check"
  - "Verify: cargo run -- --help"
  - "Verify: cargo run -- mirror --help"
  - "Add test-integration recipe to justfile if not present"
  - "Update AGENTS.md if any architecture details changed"
